# ── Dependencias de desarrollo / CI ────────────────────────────────────────
# Instalar con: pip install -r requirements-dev.txt
# No incluir en producción ni en la imagen Docker.

-r requirements.txt

# Testing
pytest>=8.0,<9.0
pytest-asyncio>=0.23,<1.0
pytest-cov>=5.0,<7.0

# Calidad de código (opcionales en CI, útiles en local)
ruff>=0.4,<1.0
